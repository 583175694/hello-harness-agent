{
  "name": "hello-harness-agent",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.11.0",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "pnpm -r --if-present build",
    "typecheck": "pnpm --filter @harness/agent-protocol --filter @harness/deepseek-v3-tokenizer build && pnpm -r --if-present typecheck",
    "lint": "pnpm -r --if-present lint",
    "test": "pnpm --filter @harness/agent-protocol --filter @harness/deepseek-v3-tokenizer build && pnpm -r --if-present test",
    "test:integration": "pnpm --filter @harness/api test:integration",
    "test:e2e": "pnpm --filter @harness/web test:e2e",
    "check": "pnpm lint && pnpm typecheck && pnpm test && pnpm build",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "setup": "node scripts/run-db.mjs setup",
    "db": "node scripts/run-db.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "eslint": "^9.39.2",
    "eslint-plugin-react-hooks": "^7.0.1",
    "eslint-plugin-react-refresh": "^0.4.26",
    "globals": "^16.4.0",
    "prettier": "^3.6.2",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.48.0"
  }
}
